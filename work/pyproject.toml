[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "ztm-config"
version = "0.1.0"
description = "Configuration module for ZTM bus analysis"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["."]
include = ["config*"]
